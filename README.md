# Folder Compressor

The idea is to use python to compress a folder of JPG files into compressed webp files, just to keep photos that you've shot.

⚠️ You need `magick` and `uv` installed on your machine.

Run using `uvx compress-images` followed by the arguments in `src/__init__.py:main`.

